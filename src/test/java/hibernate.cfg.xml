<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <!-- Database connection settings -->
    <session-factory>
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/demo</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">root</property>

        <!-- JDBC connection pool settings -->
        <property name="hibernate.c3p0.min_size">5</property>
        <property name="hibernate.c3p0.max_size">20</property>
        <!-- Other connection pool properties... -->

        <!-- Dialect specifies the SQL dialect used by your database -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all SQL to stdout for debugging -->
        <property name="hibernate.show_sql">true</property>

        <!-- Auto-create/update database schema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping files -->
        <!-- <mapping resource="com/example/YourEntity.hbm.xml"/> -->

        <!-- Annotated classes -->
        <mapping class="fullApplication.IoTDeviceLogic.model.Measurement"/>
        <mapping class="fullApplication.IoTDeviceLogic.model.Sensor"/>
    </session-factory>
</hibernate-configuration>
